package com.srpost.cm.bo.base.menu.front;

import java.util.List;
import java.util.Map;

/**
 * 내부단 프론트 메뉴관리 서비스 인터페이스
 *
 * @author  finkle
 * @date    2014-10-14
 * @since   2.0
 */
public interface IMenuFrontService {

    List<MenuFrontBean> list(MenuFrontBean bean);
    
    MenuFrontBean view(MenuFrontBean bean);

    Object insertAction(MenuFrontBean bean);

    int updateAction(MenuFrontBean bean);

    int deleteAction(MenuFrontBean bean);

    List<MenuFrontBean> listCache(MenuFrontBean bean);

    Map<Integer, MenuFrontBean> mapAllCache();
}
